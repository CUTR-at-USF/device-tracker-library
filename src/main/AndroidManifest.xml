<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.usf.cutr.trackerlib">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application android:allowBackup="false" android:label="@string/app_name">
        <receiver android:name="edu.usf.cutr.trackerlib.tracker.batch.BatchBroadcastReceiver" >
        </receiver>
        <service android:name="edu.usf.cutr.trackerlib.tracker.batch.BatchUpdateService" ></service>
    </application>
</manifest>
